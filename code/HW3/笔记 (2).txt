C语言的变量在编译时默认变量前加用下划线
NASM里不用extrn，使用extern（也可能是global）

ss，sp涉及到栈的位置

使用call进行外部调用
调用的模块不在汇编内

C分为小模式，紧凑模式，大模式等
-mt参数代表小模式，常用于com
-o后面的参数不能有空格


'>'导出记录

连接命令
/3 处理32位格式
/t 紧凑模式

ppt 10中a，b是数据段内数据，只是展示时被裁剪
bp+4为a，bp+6为b
参数压栈时，先压入最后的参数，然后逐渐往前压
（确保第一个出栈的是第一个参数
函数调用时参数不会出栈，调用完后需要手动出栈

用push，pop恢复调用前状态

call时，紧凑模式下，栈顶增加了4个字节（16位下，可能，需要查），用于ret

局部变量是运行是产生，全局变量是编译时产生
TASM存在初始化时，局部变量没有数据的BUG（有补丁（而且我觉得已经修了（）


输出elf格式，用LD连接成LD
（但是需要知道elf格式
（或者用bin，需要知道纯码跳进去